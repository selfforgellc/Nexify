# Optional heavy training dependencies.
# Install only when you are ready to fine-tune.
accelerate>=0.34.0
datasets>=2.20.0
transformers>=4.45.0
peft>=0.12.0
trl>=0.10.0
bitsandbytes>=0.43.3; platform_system != "Windows"
safetensors>=0.4.4
torch>=2.3.0
pyyaml>=6.0.0
