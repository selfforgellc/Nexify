[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "nexify-rust"
version = "0.1.0"
description = "Rust extension module for nexify"
requires-python = ">=3.10"

[tool.maturin]
manifest-path = "Cargo.toml"

